# Required in production
DATABASE_URL=
ADMIN_SETUP_KEY=

# Optional; set true only on HTTPS production deployments
COOKIE_SECURE=true

# Optional public site URL used by deployment/configuration tooling
NEXT_PUBLIC_SITE_URL=https://your-domain.example
